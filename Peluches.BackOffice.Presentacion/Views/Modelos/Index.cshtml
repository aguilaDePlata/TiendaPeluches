@model IEnumerable<Peluches.BackOffice.Presentacion.Models.Modelo>

@{
    ViewData["Title"] = "Index";
}

<h1>Inicio</h1>

<p>
    <a asp-action="Create">Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Modelo1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Modelo1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdModelo">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdModelo">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdModelo">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
