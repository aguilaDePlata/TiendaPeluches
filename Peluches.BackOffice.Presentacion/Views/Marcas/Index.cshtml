@model IEnumerable<Peluches.BackOffice.Presentacion.Models.Marca>

@{
    ViewData["Title"] = "Index";
}

<h1>Inicio</h1>

<p>
    <a asp-action="Create">Crear Nuevo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Marca1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Marca1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMarca">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdMarca">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMarca">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
